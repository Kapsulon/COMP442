// Test 3.4: Input/output statement (read/write).
// Reads three integers and writes them back doubled.

main
  local
    integer a;
    integer b;
    integer c;
  do
    read(a);
    read(b);
    read(c);
    write(a * 2);
    write(b * 2);
    write(c * 2);
  end
